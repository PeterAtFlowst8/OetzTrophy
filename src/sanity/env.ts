// Shared Sanity project configuration.
// Kept in one place so the embedded Studio (sanity.config.ts) and the
// frontend client (src/lib/sanity.ts) stay in sync.
export const projectId = 'mnazp3qy';
export const dataset = 'production';
export const apiVersion = '2024-01-01';
